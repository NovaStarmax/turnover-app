services:
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api

  api:
    build: ./api
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=dev_secret_key_change_in_production

volumes: {}
